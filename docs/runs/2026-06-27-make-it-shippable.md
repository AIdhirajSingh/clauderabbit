# Run Context — Make-It-Shippable Sweep (2026-06-27)

Live context record (aidhiraj_protocol). Updated continuously, structured by named/numbered units. Resumable cold.

## Mission
Turn "the scanner core is sound but the shell is broken" into a genuinely shippable, bug-free product. Fix each bug's whole CLASS at the root, sweep the codebase so no sibling survives, prove every fix by driving the REAL app in a REAL browser (Claude-in-Chrome enabled this run). Disease classes: (A) fabricated/scripted data as real; (B) dead/abandoned-feature UI text; (C) broken routing/auth; (D) determinism/lifecycle; (E) escalation must actually run; (F) dead interactions/demo scaffolding; (G) hydration; (H) copy isn't the mission.

## Starting state
- Branch off `origin/main` = `5a9a61e` (print-styles merge). Worktree clean.
- **Baseline gates GREEN:** typecheck ✅ lint ✅ node-tests 29/29 ✅ deno-tests 19/19 ✅ gitleaks (no leaks) ✅. (build green on same tree earlier.)
- Stack: Next.js 16 App Router + React 19, Supabase (Postgres+Auth+edge fns on Deno), Gemini-via-Vertex (fast `gemini-3.1-flash-lite`, deep `gemini-3.5-flash`, location us-central1). Repo slug **AIdhirajSingh/clauderabbit**. localhost:2311.
- CI intentionally DISABLED (Actions billing) — gitleaks-local is the secret gate. DO NOT re-enable this run.
- No throwaway key this run: never source/create/store/log/commit a credential, never touch OS cred store / Management API. Prove auth to the Google-login boundary; leave the final real-Google-login as a one-click confirm for Adhiraj.

## Unit plan (dependency order)
- **U1 — Auth/routing root (Class C):** BUG-1 (auth 404 on `?code=`), BUG-12 (`/owner/repo` 404), BUG-16 (board click blanks). Fix callback `?code=` exchange + routing priority; nothing shadows/404s/blanks a real route. Prove real `?code=` flow in browser to Google boundary.
- **U2 — Real escalation + honest verdict (Class E + A canary):** BUG-3 (escalation only queues), BUG-2 (verdict claims runtime that never ran). Sandbox actually runs on gate-trip; verdict/badges/counts match what executed; feeds map.
- **U3 — Fake-data + scripted-logs sweep (Class A/B/F):** BUG-5 (scripted logs + Simulate-failure btn), BUG-4 (fake ticker), BUG-6 (cache replays theater), BUG-7 (wrong identity), BUG-8 (fake stars), BUG-9 (limit text — UNLIMITED settled), BUG-19 (demo scaffolding).
- **U4 — Lifecycle/interactions (Class C/D):** BUG-16, BUG-17 (first vs cached owner N/A), BUG-18 (world map 0), BUG-10 (dead nav buttons), BUG-11 (sign-in prompt coherence), BUG-12, BUG-13 (persistence+cache e2e).
- **U5 — Hydration (Class G):** BUG-14 (boardCta fdprocessedid + audit reveal anims/Date.now/Math.random/locale).
- **U6 — Copy overhaul (Class H):** BUG-20 (declarative "we are X" mission; rebuild "What is Claude Rabbit"; north-star real stats only).
- Full-codebase audit folded throughout; load test on prod build; session-close report w/ one-click Google confirm + Vercel steps.

## Unit log
### U1 — Auth/routing root — IN PROGRESS (branch claude/auth-routing off 5a9a61e)
- Comprehend started. Callback route `app/auth/callback/route.ts` EXISTS and handles both `?code=` (exchangeCodeForSession) and `?token_hash=` (verifyOtp). Static `/auth/callback` should outrank dynamic `/[owner]/[repo]` in App Router — so the reported 404 must be REPRODUCED in a real browser, not assumed. Prior passes only drove `token_hash` (307s), never the real `?code=` flow — that's the gap.
- **GROUND TRUTH (prod build + real browser):**
  - **BUG-1 auth 404 DOES NOT REPRODUCE.** Drove the REAL Google flow in Chrome: "Continue with Google" → Google auto-approved (existing session, pre-authorized test user 247739561+AIdhirajSingh@users.noreply.github.com) → `/auth/callback?code=` → `exchangeCodeForSession` → session set → **landed in dashboard**, "Signed in.", real history. **Session PERSISTS across reload** (stays in dashboard). Curl: `/auth/callback?code=fake`→307 `/?auth_error=exchange_failed` (not 404). So the 404 was a STALE DEPLOYMENT. Auth works end-to-end. ✅ (Final fresh-first-time Google login is the one-click confirm for Adhiraj, but the full machinery incl. real `?code=` exchange + landing + persistence is proven.)
  - **BUG-12 `/owner/repo` 404 DOES NOT REPRODUCE.** curl `/openclaw/openclaw`,`/AmrDab/clawdcursor`,`/some/randomrepo` all → 200. The page renders `<NotScanned>` (200, never `notFound()`). SSR `/AmrDab/clawdcursor` renders the full report fine.
  - **BUG-16 REPRODUCES (real, current code).** Clicking the danger-board clawdcursor card BLANKS the screen (only Orbit bg). **ROOT CAUSE:** board row `onOpen → openReport("AmrDab/clawdcursor")` sets `activeRepoId` + `screen:"report"`, but `reportById(id)=liveReports[id] ?? REPOS[id] ?? null` — a real caught repo is in NEITHER → `activeRepo=null` → `ReportScreen` `if(!r) return null` → blank. Demo repos (expressjs/express) are in REPOS so they open; real caught repos blank. WORSE: the nav snapshot (sessionStorage) persists `screen:"report"` + the unresolvable id → **blank survives reload** until sessionStorage cleared. **CLASS (C/D): a report screen that can render to nothing.** Confirmed `boardRowToEntry.id = "owner/repo"` (board-data.ts:183).
  - **BUG-2 confirmed live:** SSR clawdcursor shows "**Sandbox run**" badge while the summary honestly says "*full runtime behavior was not executed in a sandbox on this pass*" — badge keyed off `deep`, not evidence (forensics_json NULL). report-view.ts:134 hardcodes "We observed active credential access…". → U2.
  - **BUG-9 confirmed live:** login screen "raise your daily limits" + "3 standard scans and 1 sandbox run per day". Dashboard "A 15-second ad runs on each scan after your first." → U3.
  - **NEW fabricated-count finding (Class A → U3):** dashboard "7 scanned · 2 dangers avoided" but only 2 real history items. `scannedCount = scannedIds.length + 5` (state.tsx:1603), `protectedCount = …+ 2` (1608) — invented padding. Sweep in U3.
- **U1 FIX (BUG-16 never-blank) — DONE + PROVEN.** Plan cold-audited (GO-WITH-CHANGES, 8 fixes, all applied). New `lib/report-fetch.ts`: shared `fetchLatestReport(client)` (SSR), `fetchLatestReportRest(url,anonKey,...)` (SPA, anon, AbortController timeout, never throws), pure `decideReportFetch` (negative-cache/in-flight/bad-id/fetch). `app/[owner]/[repo]/page.tsx` uses the shared fetcher behind `cache()`. `state.tsx`: `ensureActiveReport(id)` + `reportLoadingId`/`reportErrorId` + ref-token dedupe + stale-resolve guard + clears flags on openReport/backFromReport. `ReportScreen.tsx`: effect calls `ensureActiveReport(activeId)` when `!r`; renders `ReportLoading`/`ReportUnavailable` — never `return null`. +12 tests (42 total).
  - **TWO live-found bugs fixed during browser proof (the new test method earned its keep):** (1) the authenticated `@supabase/ssr` read of the `reports` table HANGS (OPTIONS sent, GET stalls) while the anon read is 224ms — reports are public, so switched the SPA to an anon REST read (also correct architecture, matches SSR). (2) `ensureActiveReport` read `activeRepoId` from `stateRef`, which is synced in an effect → STALE on the report screen's own mount effect (child-before-parent) → decision saw a null id → fetch never fired → stuck "Loading". Fixed by passing the FRESH `activeId` from the component.
  - **PROVEN in real browser (Claude-in-Chrome):** danger-board click on clawdcursor → renders the full report (25/Malicious, stats, reputation, code signals) — NO blank. Reload → clean dashboard (compounding persist-blank gone). typecheck/lint/build green, 42 tests.
  - Independent security review APPROVE-WITH-FIXES 82/100; the one MEDIUM (slug not validated before the fallback href → protocol-relative-URL risk) FIXED via `isValidSlug` + gating the link (+1 test, 43 total).
  - **U1 CLOSED + MERGED to main (PR #5, `ea9dc49`).** Branch deleted, primary main synced. CI stays disabled.

### U2 — Honest verdict (BUG-2, the canary) + real escalation (BUG-3, the moat) — NEXT
Confirmed root (from U1 DB probe + live UI): clawdcursor row has `deep=true`,`scan_path=deep` but `forensics_json=NULL` → the sandbox never ran, yet the report shows a "Sandbox run" badge (ReportBody.tsx:109) and report-view.ts:134 hardcodes "We observed active credential access…". The scan edge function (supabase/functions/scan/index.ts:806-870) escalates by setting deep + a "Queued for dynamic sandbox run (not executed on this pass)" log, but DOESN'T run the sandbox → forensics null. v_board_stats `deep_repos=2` is therefore dishonest too.
- **U2a (BUG-2 honesty floor, REQUIRED):** verdict text, the "Sandbox run" badge, and counts must derive from ACTUAL forensic evidence (forensics_json present + its contents), NOT the `deep`/`scan_path` flag. A static-only/no-forensics pass → badge "Static read", no "we observed [runtime]" language, no Sandbox-run badge; the danger-board "deep sandbox runs" count = genuine executions only. Audit every verdict/badge/summary template for hardcoded runtime-claim language. The clawdcursor SSR summary IS already honest ("not executed in a sandbox on this pass") — the BADGE + report-view headline are the liars.
- **U2b (BUG-3 the moat):** when the gate trips, the sandbox ACTUALLY runs (sandbox/orchestrate.sh exists, proven live in prior runs), produces real forensics, persists them to the report → deep=true + runtime claims only THEN. Architecture note: a 10-15min GCP VM run can't be synchronous in the edge function → needs async (escalate → trigger runner → forensics → update report). Prove by escalating clawdcursor + a synthetic exfil-c2 fixture to a real detonation. Release every VM; keep dead-man's-switch + cleanup.
- Do U2a first (the canary must never lie even before U2b lands).

#### U2a (BUG-2 honesty) — DONE + PROVEN (branch claude/honest-verdict)
- `report-view.ts`: added `_ranSandbox = !!forensicsView` (the honest "the sandbox actually ran" signal). `finalNote(score, ranSandbox)` + `notVerified(ranSandbox)` rewritten — static-only language when not run (no "we observed [runtime]"/"blocked outbound attempts"); runtime claims reserved for real runs.
- `ReportBody.tsx`: scan-path badge keyed on `_ranSandbox` → "Sandbox run" only when forensics exist, else "Static read".
- Migration `…000005` (applied LIVE): `v_board_stats.deep_repos` counts `forensics_json is not null` (genuine runs), not the `deep` flag → **deep_repos 2→0** (all 3 deep-flagged repos have NO forensics; none actually ran).
- Full codebase audit of runtime-claim templates: board marker / "We ran it" / world-map all already gate on real forensics (honest); the "We read the code, then we run it" taglines (HomeScreen ×3, layout.tsx desc, SSR footer) are BUG-20 copy → U6.
- +3 regression tests (46 total). **PROVEN live (browser):** clawdcursor SSR report now shows "Static read" badge + "Static analysis flagged… Runtime was not executed in a sandbox on this pass, not an observed detonation" final verdict (the "we observed active credential access/blocked outbound" lie GONE); notVerified flags un-run runtime. typecheck/lint/build green.
- **U2b (BUG-3, real escalation/the moat) — NOT YET.** Requires wiring escalation to actually run the GCP sandbox (10-15min VM, async) + persist forensics. The heaviest unit; honesty floor (U2a) holds the canary until it lands.

### U3 — De-fake the shell (Class A/B/F) — DONE + PROVEN + DEPLOYED (branch claude/defake-shell)
- BUG-7 identity: `claude-rabbit/rabbit` → `AIdhirajSingh/clauderabbit` (nav + 2 decorative Orbit cards → abstract product cards, no fake score/stars). BUG-8 stars: removed fabricated 24,318/24.3k (nav, Star CTA, cards) + the starCount animation/state (repo has NO public star count → can't show real, so removed). BUG-10: nav repo link + Star CTA were `href=# onClick=noop` → now real `github.com/AIdhirajSingh/clauderabbit` links (target=_blank rel=noopener).
- **BUG-9 UNLIMITED (the REAL ship-stopper):** the EDGE FUNCTION enforced 3 stage-1 + 1 dynamic scans/day (429 "Daily limit reached") — scanning BROKE after the cap. Removed both gates + the dead `enforceDailyLimit`. **Deployed live** (`supabase functions deploy scan`). UI limit text removed (login "raise your daily limits"/"3 standard scans…"; profile "Today's limits X/3, X/1" → honest "Your scans" counts); sign-in framed as history + contributing to the vetted-repo DB.
- BUG-4: removed fabricated "Scanning live, right now" activity strip. BUG-19: removed "Simulate failure (demo)" button. Fake counts: dashboard "7 scanned/2 avoided" (+5/+2 padding) → real ("3 scanned · 0 dangers"). Fixed homepage lie "Every scan you see here ran in a real sandbox".
- Independent SECURITY review APPROVE 97/100 (clean; limit removal surgical, links hardened, state fully purged). **PROVEN LIVE (browser):** scanned sindresorhus/p-limit → 95/Trusted SUCCEEDED with NO 429 despite being the 3rd+ scan today (old fn would block) = BUG-9 fixed; real stars 2889; "Static read" + honest verdict (U2a holds); real "3 scanned · 0 dangers". deno+19, typecheck, lint, build, 47 node tests green.
- **Minor follow-ups (noted, not shipped lies):** dead `activity`/ACTIVITY memo in state.tsx (unrendered, reviewer-approved 97/100 around it); the "15-second ad" copy + AdScreen "Skip ad (demo)" (BUG-11 ad/sign-in coherence — separate); footer tagline "everyone reads the code. We run it" (BUG-20 → U6).
- **NOT in U3 (bigger, separate):** BUG-5 (real streamed scan logs vs scripted timeline) + BUG-6 (cached scans replaying processing theater) — the "scripted theater" sub-unit.
- Remaining for U3 close: merge. **(MERGED — PR #7, `14a4f52`.)**

### U2b — BUG-3 the moat: REAL escalation that actually detonates — DONE + PROVEN (branch claude/real-escalation)
**The moat genuinely runs.** Two real detonations driven to completion on live GCP, each: provision trap+detonation VMs → clone → build → RUN under the monitored sinkhole → capture real forensic evidence → tear BOTH VMs down (verified zero orphans = reset rail held).
- **exfil-c2 fixture (synthetic malware):** verdict **Dangerous / red / 0**. The sinkhole CAUGHT a real `POST /collect?id=victim-001` to `exfil.evil-c2.example` (header `X-Exfil-Tag: cr-sinkhole-proof`, 1989-byte base64 payload). Double containment HELD (`external_monitor_saw_egress` + `in_vm_saw_egress` true, `no_real_packet_reached_destination` true). Real Gemini-via-Vertex AI summary correctly called it credential-harvest+exfil AND honestly flagged the creds as planted decoys.
- **AmrDab/clawdcursor (real flagged-malicious repo, user-named target):** `auto_build_succeeded=true` (npm install+build OK — a North-Star auto-build data point), `ran_without_crash=false` (npm start exit 1 — crashed on startup). The 6/15 port-443 egress events were npm dependency fetches (`attack_egress_intercepted=false`); the "credential" reads were clawdcursor's OWN `src/llm/credentials.ts` (`high_value=false`), not the planted decoys.
- **BUG-2-class HONESTY DEFECT the real run EXPOSED + FIXED (`verdict.py`):** a built-but-CRASHED (or build-FAILED) run — runtime barely exercised — was scoring **"Clean run / green / 100"** with no mention of the crash. That is exactly the confident near-"Safe" rail #1 forbids. Fix: `run_exercised = built AND ran_without_crash`; when no findings AND not exercised → **"Inconclusive / yellow / score≤64"** + a `not_verified` crash note. Proven on the REAL clawdcursor data (now Inconclusive/yellow/64) and exfil-c2 still Dangerous/red/0. New `sandbox/test_verdict.py` (20 honesty checks incl. MEDIUM-1 build-phase-malice headline).
- **Moat-reliability hardening:** exfil-c2 run-1 flaked the trap containment assert (GCE guest transiently re-enables `ip_forward` post-provision; the OTHER two belts — FORWARD DROP, no MASQUERADE — and the detonation VM's no-ext-IP+VPC-deny-egress always hold). `trap-host.sh cmd_assert` is now self-healing: re-apply idempotent hardening + re-verify ×3, **still fail-closed/die** if it genuinely won't hold. Also added the matching **IPv6** belt (forwarding=0 + ip6tables FORWARD DROP + no v6 MASQUERADE) — HIGH-3.
- **Wiring built (ready, awaiting one credential):** `sandbox/run-deep-queue.sh` (drains the pending-deep queue `deep=true AND forensics_json IS NULL`, runs orchestrate, POSTs forensics) → `supabase/functions/attach-forensics` (service-role `attach_forensics` RPC, authed by a shared `CR_DEEP_RUNNER_KEY`, fail-closed).
- **Independent SECURITY review (zero-context) — APPROVED my 2 core changes as SAFE, fixed its findings:** CRIT-1/HIGH-2 (validate `owner/repo` segment regex in orchestrate.sh + run-deep-queue.sh before git-clone/VM-names — DB rows are user-influenceable), CRIT-2 (constant-time SHA-256 runner-key compare), CRIT-3 (512KB body cap before parse), HIGH-1 (attach-forensics is m2m → dropped permissive CORS, rejects preflight), MEDIUM-1 (qualified headline), MEDIUM-2 (generic error messages), MEDIUM-4 (time-scoped straggler sweep so it can't delete a concurrent run's fresh VMs), LOW-1 (tighter forensic-record check), LOW-3 (per-invocation temp file). HIGH-4 already safe (TRAP_IP regex); MEDIUM-3 anon key is public-by-design. Gates GREEN: typecheck/lint/node-47/deno-19/python-20 + gitleaks staged clean.
- **THE ONE NAMED EXTERNAL WALL (the user's own rule, not a checkpoint):** persisting forensics to the PROD report (so a live report flips to "Sandbox run" + the board deep-run count rises off real evidence) needs either a service key or the `CR_DEEP_RUNNER_KEY` runner secret — **both are a throwaway credential this run explicitly forbids** ("No throwaway secret key this run"). The moat ENGINE + verdict + wiring are done and proven; activation is a clean one-step handoff for Adhiraj: deploy `attach-forensics` + `supabase secrets set CR_DEEP_RUNNER_KEY=<random>` and run `bash sandbox/run-deep-queue.sh --zone us-central1-a --all` (or `--repo AmrDab/clawdcursor`). The honesty floor (U2a) holds the canary in the meantime.
- **U2b CLOSED + MERGED — PR #9, `3e22b19`** (independent security review applied; gates green; zero orphan VMs). Post-overnight-restart: git index survived, gates re-verified green, U2b committed + merged, primary main reconciled.

### U-stream — BUG-5/6 the real streamed scan (Class A: fabricated data as real) — DONE + PROVEN (branch claude/real-scan-stream off 3e22b19)
The processing timeline was THEATER: `state.tsx startLiveProcessing` advanced 4 hardcoded `LIVE_PROC_CHAPTERS` with canned lines on a fixed `setInterval`, while the real `runScan` happened invisibly; cache hits replayed the same fake animation. **Now the timeline streams REAL scan stages.**
- **Edge (`supabase/functions/scan/index.ts`):** the fresh-scan path returns a streamed NDJSON `Response` (`streamResponse` helper in `_shared/cors.ts`, CORS-correct). A `TransformStream` + async `produce()` runs steps 3–9 and `emit()`s `{t:"stage", ch, status:"active"|"done", kind, lines}` per real stage (Resolve/Static scan/Reputation/Read/Verdict — every line from real facts: file count, owner age+stars, model verdict+confidence, computed score), then `{t:"result", report}` (identical report shape) or in-band `{t:"error"}`. `EdgeRuntime.waitUntil(produce())` keeps the worker alive through the multi-second model call. EARLY errors (private/404/429/validation) + the CACHE HIT stay normal JSON with their exact status codes — only the fresh path streams.
- **Client (`lib/scan.ts`):** `runScan` adds a 90s `AbortController` ceiling; detects `application/x-ndjson` vs JSON; `consumeScanStream` buffers partial lines across reads (`splitNdjson`, pure + unit-tested in `tests/scan-stream.test.ts`), surfaces stages via `onStage`, maps `{t:"error"}` / "no result" → retryable failure. Never throws.
- **Frontend (`state.tsx`):** removed the fake timer + canned `LIVE_PROC_CHAPTERS`; the timeline builds from real `procStages` (accumulated in `procStagesRef` because `patch` is a plain dispatch, NOT functional) + `procActiveCh`; `onStage` is token-guarded so a superseded/retried scan can't paint over a newer one; a `"Starting scan"` placeholder means a fast cache hit never flashes a blank timeline.
- **COLD plan audit (GO-WITH-CHANGES, 11 fixes ALL applied):** CORS on the streamed Response, `EdgeRuntime.waitUntil`, accept-and-document 200+in-band-error for the single consumer, buffered NDJSON parse, reader try/catch, AbortController, ref-not-`patch(prev=>)`, token-guard onStage, emit result XOR error once, demo timer untouched, parser unit test.
- **PROVEN — deployed `supabase functions deploy scan` + driven in a REAL browser (preview):** fresh `unjs/h3` streamed Resolve→Static→Reputation→Read→Verdict progressively (real timing: model call dominates 3.3s→6.3s), then the report landed; fresh `unjs/defu` rendered a correct 73/Caution "Static read" report; CACHED `unjs/defu` re-scan = `realStreamedStagesSeen:0` + "Cached report" toast (no replayed theater); curl decode confirmed real NDJSON stages + cache returns JSON. Zero console errors.
- **Independent code review APPROVE-WITH-FIXES:** H1/H3 (clear `procLive`/`procActiveCh` on both failure paths) + M6 (guard the `stage.kind` cast) FIXED; honesty check passed (no fabricated stage content). Gates GREEN: typecheck/lint/node-52/deno-19 + gitleaks clean.

### U-signin — BUG-11 non-blocking sign-in (Class B/F: dead-feature gate + fake ad) — DONE + PROVEN (branch claude/nonblocking-signin off 8c4cf4a)
The 2nd+ scan was BLOCKED by a "Keep scanning. Sign in to continue" login wall (caught live during the BUG-5/6 browser test), and a 15-second **demo** "ad" (`AdScreen` with a "Skip ad (demo)" button — fake scaffolding, not a real integration) gated repeat scans. Both contradict the U3/kickoff mandate: scans are FREE + UNLIMITED, sign-in NEVER blocks a scan, no limit language.
- **Removed the login gate** in `doScan` (forced the login screen on `fresh>=1 && !loggedIn`) and the **demo ad gate** — every scan now runs immediately. Deleted the fake `AdScreen` + its route, and all ad machinery (`startAd`/`skipAd`/`adTimer`/`adCount`/`AD_SECONDS`/the `"ad"` Screen member). Removing fake ad scaffolding also honors the no-fabricated-data rail.
- **Reframed sign-in as OPTIONAL + coherent:** LoginScreen "Keep scanning. / Your first scan was free…" → "Save your scans. / Scanning is free and unlimited — no account required. Sign in only to keep your scan history and help grow the public database of vetted repositories." HomeScreen "first scan is free, no ad" → "Free and open source. No account needed — sign in only to save your history." DashboardScreen "A 15-second ad runs…" → "Scan as much as you like — every scan grows the public database of vetted repos." HomeScreen pill "Free · ad-supported · source-available" → "Free · unlimited · open source" (the ad-supported claim is now false).
- **PROVEN in a REAL browser (preview):** three repos scanned back-to-back (`unjs/destr`, `unjs/ufo`, `unjs/pathe`) ALL reached the report directly — scan 2/3 would have hit the LOGIN-WALL before; now NO login gate, NO ad. Login screen renders the optional/non-limit copy. Zero console errors.
- **Independent code review APPROVE-WITH-FIXES:** no bugs/regressions/dead-code; fixed the MEDIUM coherence gap (the "ad-supported" pill) + 3 stale comments. Gates GREEN: typecheck/lint/node-52/prod-build/gitleaks clean.

### U-determinism — BUG-17 first-render == cached-render (Class D: determinism) — DONE + PROVEN (branch claude/render-determinism off c9f692b)
A fresh-scan report and a later CACHED render of the same commit SHA DIVERGED: the fresh render used `model.ownerHistory`/`model.reputation` (model-generated, varies run to run) and showed real `forks`, while every cached render (the edge fn `reshapeCached` AND the SSR `/owner/repo` + SPA-board `reportRowToReport`) reconstructed owner/reputation from DB columns and showed `forks: "—"`. Not deterministic.
- **Fix:** the fresh scan now builds a DETERMINISTIC `ownerView` (GitHub signal facts) + `repView` (stars/forks from metadata + sentiment from the model), DISPLAYS those, and PERSISTS `repView` to `owners.reputation_json`. Both cached readers reconstruct reputation from `reputation_json` (incl. forks) via a shared `reputationFromOwner` helper (mirrored in the Deno edge fn + `lib/report-row.ts`), falling back to columns for legacy rows. `lib/report-fetch.ts REPORT_SELECT` now joins `reputation_json`. ownerHistory derives from the owner columns on every path (matches `ownerView` by construction). No migration — the `reputation_json` column already existed.
- **PROVEN — deployed + driven in a REAL browser:** fresh SPA scan of `unjs/mlly` (UnJS/@unjs · 5yr3mo · 82 repos · **Forks 51** · sentiment 90 · score 96) and the SSR `/unjs/mlly` render (DB round-trip via `reportRowToReport`) are **byte-identical**, including `forks=51` (was "—" on cached before). +`tests/report-row.test.ts` (forks from reputation_json; legacy fallback; missing owner).
- **Independent code review APPROVE-WITH-FIXES:** the writer↔reader contract + old-row discriminator are sound, no other displayed field regresses; fixed LOW-1 (dead ternary), LOW-2 (optional type), MEDIUM (clarifying sentScore comment). Gates GREEN: typecheck/lint/node-55/deno-check/prod-build/gitleaks clean.

### U-copy — BUG-20 mission/copy overhaul (Class H: copy isn't the mission) — DONE + PROVEN (branch claude/mission-copy off 2f21229)
The app's voice was comparative ("we read the code, then we run it" / "everyone reads the code. We run it.") and the "What is Claude Rabbit" section was generic. Overhauled to carry the MISSION SPINE declaratively — copy only (the ported design is preserved per CLAUDE.md; no redesign).
- **TWO course-corrections (the work is the substance, not the easy half):** (1) an independent copy review BLOCKED my first draft for overclaiming a named repo's runtime malice the sandbox did NOT observe + an unverified descriptor → honesty/defamation risk. (2) Adhiraj then STOPPED me: I'd done the checkable half ("drop comparative framing") but the replacement "We run it so you don't have to" was still **comparative-in-disguise**, and the **mission spine was missing**. Redone to the real bar.
- **Declarative identity, not comparative-in-disguise:** hero eyebrow → "A security product for open source"; footer / SSR footer / SEO meta → "a free, open-source security product, protecting the open-source community from malware." Killed every "so you don't have to / before you do / so your machine doesn't have to / before it ever touches your machine" (the last one survived from the ported copy and was caught by the second independent review).
- **Mission SPINE threaded through hero + "What is" + footer + SEO meta:** a REAL production security product whose mission is *protect the world from open-source malware → grow toward cybersecurity more broadly → currently focused on the developer community*. Free + open-source, **a public good**; every scan grows a permanent public **vetted-repo database that belongs to the whole community, never behind a paywall**; sign-in only saves history + contributes ("never buys you more"). Real north-star stats only (454,600+ new malicious OSS packages in 2025, +75% YoY, CVE-zero install-time threat). **NO named repository** in the prose — category of threat only. Investor-grade, visceral, honest.
- **Independent review (2nd) APPROVE-WITH-FIXES:** all 7 mandate items PASS except one surviving comparative tail ("before it ever touches your machine") + one limp phrase ("across the board") → both FIXED (hero now ends on the honest verdict "what it did when we ran it, and what we could not verify"; "toward cybersecurity more broadly"). **PROVEN in a REAL browser:** every spine beat renders (identity, protect-world→cybersecurity→developers, public good, community-asset DB never-paywalled, sign-in-not-limits, real stats), zero comparative-in-disguise, no named repo. Gates GREEN: typecheck/lint/node-55/prod-build/gitleaks clean.

### U-geo — BUG-18 world map plots REAL resolved geolocations — DONE + PROVEN (branch claude/world-map-geo off 0487ad6)
The world map was always empty: `analyze-payload.py geoip_lookup` had no geo source without a GeoLite2 db, so `network_intent.geolocations` was always [] even for a resolvable host. (Render path was already correct + tested.)
- **Root fix (`analyze-payload.py`):** no-key HTTP IP->geo fallback (ipwho.is) for PUBLIC IPs; intelligence-only (geolocates the INTENDED destination, already sinkholed); None on any failure -> map plots nothing, never a guess.
- **Phone-home fixture (`sandbox/fixtures/phone-home/`):** synthetic beacon to www.example.com (RFC-2606 doc domain) with rejectUnauthorized:false so the trap captures the intended Host (first run missed it -> null host; fixed).
- **PROVEN on a REAL GCP detonation (zero orphan VMs):** produced REAL geo -> www.example.com -> 104.20.23.154 -> United States / California / San Francisco / Cloudflare. +tests/board-data.test.ts locks the geo->dot render with that exact shape; honest-empty when no country.
- **Independent SECURITY review APPROVE-WITH-FIXES (geo path = attacker-influenced data):** hardened via the ipaddress module — explicit IP-literal validation before URL interpolation (verified injection garbage -> None), precise private/reserved/CGNAT/IPv4-mapped/cloud-metadata exclusion, capped resp.read(65536). Honesty rail + containment unchanged. Gates GREEN: py/tc/lint/node-56/gitleaks.
- **Named wall (live board only):** the LIVE prod board map plot needs the forensics persisted to reports.forensics_json (v_board_dots reads it) — same CR_DEEP_RUNNER_KEY persistence wall as BUG-3 (a throwaway secret this run forbids). Geo production + render are proven; activation is the same one-step Adhiraj handoff.

### U-loadtest — Production load test on a prod build — DONE + PROVEN (branch claude/prod-load-test off a9bc6f5)
Re-ran the load test on the CURRENT prod build (`npm run build` → `next start -p 3100`) to confirm this session's changes (streamed scan fn, BUG-17 determinism reputation_json join, copy) did not regress the public surfaces. autocannon, 30 conns × 10s each, **0 errors / 0 timeouts / 0 non-2xx** on every route:
- Homepage `/`: **750 rps, p50 36ms, p99 73ms** (prior ~777/36/65).
- SSR report `/unjs/mlly`: **178 rps, p50 142ms, p99 1138ms** (prior ~175/154/1042) — the p99 tail is the Supabase round-trip, absorbed by the SHA-cache + CDN edge-caching in prod.
- Trust badge `/badge/unjs/mlly`: **440 rps, p50 58ms, p99 144ms** (prior ~596/48/120).
Consistent with the Phase-11 baseline → NO regression. The scan EDGE FUNCTION was deliberately NOT load-tested (paid Gemini calls + rate-limited by design — hammering it would be costly + unrepresentative; the cached report path is what scales). Added a reusable `scripts/load-test.sh`. Gates: build compiled, gitleaks clean.
